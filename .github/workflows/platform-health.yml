name: Platform Health Check
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  health:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - name: Check ACLClouds health
        env:
          ACLCLOUDS_HEALTH_URL: ${{ secrets.ACLCLOUDS_HEALTH_URL }}
        run: |
          set -e
          curl --fail --silent --show-error --max-time 20 "$ACLCLOUDS_HEALTH_URL"
          echo ''
          echo '✅ ACLClouds health check passed'
      - name: Check Wasmer health
        run: |
          set -e
          curl --fail --silent --show-error --max-time 25 "https://wx-node-test.wasmer.app/health"
          echo ''
          echo '✅ Wasmer health check passed'
